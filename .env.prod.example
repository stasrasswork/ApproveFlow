# Copy to .env.prod for docker compose -f docker-compose.prod.yml --env-file .env.prod up
JWT_SECRET=change-me-to-a-long-random-string
POSTGRES_PASSWORD=postgres
CORS_ORIGIN=http://localhost:8080
WEB_PORT=8080
