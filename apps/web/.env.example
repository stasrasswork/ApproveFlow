# Empty = use Vite dev proxy (/api → localhost:3000)
VITE_API_URL=

# Production — pick one:
# Same origin (nginx proxies /api):  VITE_API_URL=/api
# Separate API host:                 VITE_API_URL=https://api.example.com
