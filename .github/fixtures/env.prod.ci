# CI fixture for `npm run check:env:prod` / release preflight.
# Not used for real deploys.
JWT_SECRET=ci-release-secret-at-least-32-characters
POSTGRES_PASSWORD=ci-release-postgres-password
CORS_ORIGIN=http://localhost:8080
APP_URL=http://localhost:8080
NODE_ENV=production
EXPOSE_DEBUG_TOKENS=false
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
REDIS_URL=
WEB_PORT=8080
